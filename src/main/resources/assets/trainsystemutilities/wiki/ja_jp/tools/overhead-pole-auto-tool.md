---
title: 架線柱自動配置ツール
id: tools/overhead-pole-auto-tool
tags: [electrification, tool]
---

# 架線柱自動配置ツール

```embed:item id=trainsystemutilities:overhead_pole_auto_tool size=48 label=true
```

線路上を右クリックした地点に[架線柱](../electrification/overhead-pole.md)を配置する、**手に持って使うツール**です。まず設定画面で柱の高さ・離隔・複線数などを決め、次に配置モードに切り替えて線路を右クリックすると、その地点に柱が 1 基ずつ立ちます。

## 開き方 / 持ち方

このツールは **2 つのモード**を持ちます。持ち替えた直後は「GUI モード」（設定画面）です。

- **GUI モード**（初期状態）: 右クリックで**設定画面**が開きます。柱の高さ・線路からの離隔・設置間隔（スパン）・複線数・トラス / 碍子の使用などをここで調整します。
- **選択モード**: 線路を右クリックして**実際に柱を配置**するモードです。

モードの切り替えは **Alt + マウスホイール**で行います（下記「操作」を参照）。持っている間、ホットバーの上に現在のモードが表示されます。

## 使い方

1. ツールを持ち、そのまま**右クリック**（初期設定では**マウス右ボタン**）して**設定画面**を開きます。
2. 設定画面で、柱の高さ・離隔・複線数・トラス / 碍子などを決めます（設定画面の操作は[架線柱](../electrification/overhead-pole.md)を参照）。
3. **Alt + ホイール**で**選択モード**に切り替え、さらに **Ctrl + ホイール**で**「配置する」**サブモードにします。
4. 立てたい**線路を右クリック**すると、その地点に柱が 1 基配置されます（マウスを線路に向けている間は半透明のプレビューが出ます）。
5. 素材が足りないときは赤字で「素材が足りません」と表示され、配置されません。

> [!NOTE]
> 「配置する」サブモードで**線路以外**を右クリックすると「線路を右クリックしてください」と表示され、配置されません。必ず Create のトラックを狙ってください。

## 操作 (ホイール / クリック)

| 入力 | 動作 |
|---|---|
| **右クリック**（GUI モード） | 設定画面を開く |
| **右クリック**（選択モード / 配置する） | 狙った線路の地点に柱を 1 基配置 |
| **Alt + ホイール** | ツールモード切替（GUI ⇔ 選択）。※ただし「配置する」サブモード中は、柱の向きを 8 方向（45° ずつ）に回転 |
| **Ctrl + ホイール** | 選択モード内のサブモード切替（GUI へ戻る / 配置する） |
| **Shift + ホイール** | 現在の編集対象（高さ / クリアランス / スパン）の値を増減 |
| **Shift + 中クリック** | 狙ったチェストを**素材倉庫としてリンク**（そこから柱の素材を自動で引き出す） |

> [!TIP]
> 高さ・離隔・スパンは、**設定画面**でも、**Shift + ホイール**（持ったまま）でも調整できます。  
> カント (傾き) のある曲線や複線区間では、設定画面のトラスオプションで[架線トラス](../electrification/overhead-truss.md)を使う構成に切り替えられます。

## 関連ページ

- [架線柱](../electrification/overhead-pole.md) / [架線トラス](../electrification/overhead-truss.md)
- [電化システム概要](../electrification/index.md)
